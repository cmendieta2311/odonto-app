# Database Configuration
POSTGRES_USER=sgodonto
POSTGRES_PASSWORD=password123
POSTGRES_DB=sgodonto_db

# Application Configuration
BACKEND_PORT=3000
FRONTEND_PORT=80
CORS_ORIGIN=http://localhost

# Security
JWT_SECRET=supersecretkey

# Prisma Configuration (for backend service)
# This is constructed from the DB variables for convenience in this example, 
# but in production you might set the full URL directly.
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}?schema=public
